<?xml version="1.0" encoding="utf-8"?><!--<shape xmlns:android="http://schemas.android.com/apk/res/android"--><!--android:shape="rectangle">--><!--<corners android:radius="2dp" />--><!--<stroke android:width="2px" android:color="@color/md_white_1000" />--><!--</shape>-->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?android:colorControlHighlight">

    <!-- Note: <ripple> acts like a layer-list -->
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <!-- This color is not displayed in any way -->
            <solid android:color="@android:color/black" />
        </shape>
    </item>

    <!-- This is the border -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="3dp" />
            <!-- Use your border color in place of #f00 -->
            <stroke
                android:width="1dp"
                android:color="@color/md_white_1000" />
        </shape>
    </item>
</ripple>